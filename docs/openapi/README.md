# OpenAPI Spec Location

Place the authoritative OpenAPI spec at `docs/openapi/openapi.yaml`.

This file is used by the API client generator:

```bash
npm run generate --workspace ./packages/api-client
```
